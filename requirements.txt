# Pinned together: the playwright package downloads a Chromium build matched
# to its own version, so bumping one without the other breaks the launch.
# The workflow runs `python -m playwright install --with-deps chromium`.
requests>=2.31
playwright==1.49.0
